没关系，我来给你拆解思路，但代码还是你来写。

---

# 拆解步骤

**第一步：怎么读入这个字符串？**

输入是 `HeLLo WorLD`，中间有空格，所以不能用 `cin >>`（它遇到空格会停）。

要用什么？想想我刚才讲的。

---

**第二步：怎么遍历字符串里每个字符？**

我刚才讲过遍历字符串的写法：

```cpp
for (char c : s) {
    // 对每个字符做点什么
}
```

---

**第三步：怎么判断一个字符是大写？**

我刚才也讲过，有一个现成的函数可以直接用，叫什么来着？

---

**第四步：怎么计数？**

定义一个变量 `count = 0`，每发现一个大写字母就 `count++`，最后输出。

---

# 把四步串起来

```
读入字符串
→ 遍历每个字符
  → 如果是大写字母
    → count加一
→ 输出count
```

按这个思路写，把代码发给我！
