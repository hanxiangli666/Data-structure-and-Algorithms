这道题我刚才讲过，直接拆解给你：

---

# 四步走

**第一步：读入数据**

```
读一个整数 n
读 n 个整数存进 vector
```

这个你在第一课学过，模式一：读固定数量。

---

**第二步：排序**

刚才讲过，一行代码，用 `sort`。

---

**第三步：去重**

刚才讲过，两行代码，`unique` + `erase`。

---

**第四步：输出**

遍历 vector，用 `cout` 打印每个元素。

---

这四步全都是我刚讲过的内容，直接套就行。

动手写吧，不会就对着上面的笔记找，这个过程本身就是学习。
