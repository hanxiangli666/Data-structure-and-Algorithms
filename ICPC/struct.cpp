#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

struct Student
{
    string name;
    double score;
}

cin >> n;
vector<Student> students[n];
for (int i = 0; i < n; i++) 
{
    cin >> s[i];
}

int main() {
    if (a.score != b.score) 
    {
    sort(students.begin(), students.end(), [](Student &a, Student &b) {
        return a.score > b.score;
    })
    }   
    else
    {
        sort()
    }
    return 0;
}